language: bash

before_script:
  - sudo add-apt-repository -y "deb http://mirrors.kernel.org/ubuntu utopic main universe"
  - sudo apt-get -y update
  - sudo apt-get -y install shellcheck

script:
  - shellcheck lib/shell.sh
  - shellcheck lib/helpers.sh

notifications:
  email: false
